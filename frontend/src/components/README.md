# Shared components

- `ui`: primitive không chứa nghiệp vụ.
- `layout`: shell/navigation dùng chung.
- `data-display`: composition hiển thị dữ liệu dùng từ nhiều feature.
- `scaffold`: placeholder tạm thời, phải xóa sau khi các route được triển khai.
