# Feature modules

Mỗi role sở hữu feature tree riêng. Chỉ tạo `components`, `hooks`, `queries`, `schema` hoặc `types` bên trong một feature khi bắt đầu triển khai feature đó. Xem `frontend/STRUCTURE.md` để biết dependency rules.
