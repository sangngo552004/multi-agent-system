package com.tttn.backend_core.entity;

/** The structured CV field a bonus rule is allowed to inspect. */
public enum InstitutionalEvidenceSource {
  EXPERIENCE,
  EDUCATION,
  CERTIFICATION,
  GPA
}
